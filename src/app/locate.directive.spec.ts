import { LocateDirective } from './locate.directive';

describe('LocateDirective', () => {
  it('should create an instance', () => {
    const directive = new LocateDirective();
    expect(directive).toBeTruthy();
  });
});
